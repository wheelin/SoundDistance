<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobop.sounddistance" >

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="mobop.activities.ResultListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="mobop.activities.MeasureChoiceActivity"
            android:label="@string/title_activity_measure_choice" >
        </activity>
        <activity
            android:name="mobop.activities.MeasuringActivity"
            android:label="Measure Control" >
        </activity>
        <activity
            android:name="mobop.activities.BluetoothActivity"
            android:label="@string/title_activity_bluetooth" >
        </activity>
        <activity
            android:name="mobop.activities.MeasureResultActivity"
            android:label="@string/title_activity_measure_result" >
        </activity>
    </application>

</manifest>
